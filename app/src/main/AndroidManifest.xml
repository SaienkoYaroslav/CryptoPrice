<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.com.app.saienko.yaroslav.cryptoprice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/coin_price_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/coin_price_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CryptoPrice">
        <activity
            android:name=".CoinDetailActivity"
            android:exported="false" />
        <activity
            android:name=".CoinPriceListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>